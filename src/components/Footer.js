import React from 'react';

const Footer = () => {
    return (
        <div style={{background:'black', padding:'10px',textAlign:'center' }}>
            <p style={{color:'#fff', fontSize:'2rem'}}>Footer of the site</p>
        </div>
    );
};

export default Footer;